# GitHub Pages 部署指南

## 📋 前置要求

1. 已在 GitHub 上创建仓库
2. 本地项目已初始化 Git
3. 已配置 GitHub 远程仓库

## 🚀 部署步骤

### 1. 配置 GitHub Pages

在 GitHub 仓库中进行以下设置：

1. 进入仓库的 **Settings** (设置)
2. 在左侧菜单找到 **Pages**
3. 在 **Build and deployment** 部分：
   - **Source**: 选择 `GitHub Actions`

### 2. 修改 base 路径（重要！）

在 `vite.config.js` 中，将 `base` 设置为您的仓库名：

```javascript
base: process.env.NODE_ENV === 'production' ? '/你的仓库名/' : '/',
```

**示例**：
- 如果仓库名是 `yitu-demo`，设置为 `'/yitu-demo/'`
- 如果仓库名是 `my-project`，设置为 `'/my-project/'`

### 3. 推送代码到 GitHub

```bash
# 添加所有文件
git add .

# 提交更改
git commit -m "配置 GitHub Pages 自动部署"

# 推送到 main 分支
git push origin main
```

### 4. 查看部署状态

1. 进入仓库的 **Actions** 标签页
2. 查看 "部署到 GitHub Pages" 工作流的运行状态
3. 等待构建和部署完成（通常需要 1-3 分钟）

### 5. 访问网站

部署成功后，您的网站将在以下地址可用：

```
https://<你的用户名>.github.io/<仓库名>/
```

**示例**：
- 用户名：`zhangsan`
- 仓库名：`yitu-demo`
- 网站地址：`https://zhangsan.github.io/yitu-demo/`

## 🔄 自动部署

配置完成后，每次推送代码到 `main` 分支时，GitHub Actions 会自动：

1. ✅ 检出代码
2. ✅ 安装依赖
3. ✅ 构建项目
4. ✅ 部署到 GitHub Pages

## 🛠️ 手动触发部署

如果需要手动触发部署：

1. 进入仓库的 **Actions** 标签页
2. 选择 "部署到 GitHub Pages" 工作流
3. 点击 **Run workflow** 按钮
4. 选择分支并确认

## 📝 注意事项

### 1. 分支名称

如果您的主分支不是 `main` 而是 `master`，需要修改 `.github/workflows/deploy.yml`：

```yaml
on:
  push:
    branches:
      - master  # 改为 master
```

### 2. Node.js 版本

工作流使用 Node.js 18，如果需要其他版本，修改 `.github/workflows/deploy.yml`：

```yaml
- name: 设置 Node.js
  uses: actions/setup-node@v4
  with:
    node-version: '20'  # 改为需要的版本
```

### 3. 构建输出目录

默认构建输出到 `dist` 目录，如果不同，修改 `.github/workflows/deploy.yml`：

```yaml
- name: 上传构建产物
  uses: actions/upload-pages-artifact@v3
  with:
    path: './build'  # 改为实际的输出目录
```

### 4. 路由模式

如果使用 Vue Router 的 history 模式，GitHub Pages 可能无法正确处理路由。建议：

- 使用 hash 模式（默认）
- 或添加 404.html 重定向配置

## 🐛 常见问题

### 问题 1: 页面显示 404

**原因**：`base` 路径配置不正确

**解决**：检查 `vite.config.js` 中的 `base` 是否与仓库名匹配

### 问题 2: 样式和资源加载失败

**原因**：资源路径不正确

**解决**：确保 `base` 配置正确，并且使用相对路径引用资源

### 问题 3: Actions 权限错误

**原因**：GitHub Pages 权限未正确配置

**解决**：
1. 进入 Settings → Actions → General
2. 在 "Workflow permissions" 中选择 "Read and write permissions"
3. 勾选 "Allow GitHub Actions to create and approve pull requests"

### 问题 4: 部署成功但页面空白

**原因**：路由配置或 base 路径问题

**解决**：
1. 检查浏览器控制台错误
2. 确认 `base` 路径正确
3. 检查路由配置

## 📚 相关文档

- [Vite 部署文档](https://vitejs.dev/guide/static-deploy.html)
- [GitHub Pages 文档](https://docs.github.com/en/pages)
- [GitHub Actions 文档](https://docs.github.com/en/actions)

## 🎉 完成

配置完成后，您的项目将自动部署到 GitHub Pages！每次推送代码都会触发自动构建和部署。

